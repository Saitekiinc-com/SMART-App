/**
 * S-06: ゴール詳細
 * GoalDetailPanel, VoteUI, StatusDisplay 等
 */
export default function GoalDetailPage({
  params,
}: {
  params: Promise<{ goalId: string }>;
}) {
  return (
    <main>
      <p>ゴール詳細（S-06）</p>
    </main>
  );
}
